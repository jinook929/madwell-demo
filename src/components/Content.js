const Content = () => {
    return (
        <div className="Content">
            <p>I'm centered vertically on desktop, but flush with a right edge of some sort.</p>
        </div>
    )
}

export default Content
